# ──────────────────────────────────────────────
#  Production dependencies — all versions pinned
#  for fully reproducible builds.
# ──────────────────────────────────────────────

# Web framework
fastapi==0.115.5

# ASGI server (standard extras include websockets + httptools for performance)
uvicorn[standard]==0.32.1

# Settings management via environment variables
pydantic-settings==2.6.1

# HTTP client used by TestClient in tests (optional at runtime, required for tests)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
